# from projectdir import database
#
#
# class User(database.Model):
#     id = db.Column(database.Integer, primary_key=True)
#     usernam = database.Column(db.String(20), unique=true, nullable=False)
#     email = database.Column(db.String(120), unique=true, nullable=False)
#     image_file
#     password
#     data_created

# class Log(database.Model):
    # id = database.Column(database.Integer, primary_key=True)
    
    